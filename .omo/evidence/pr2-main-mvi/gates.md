# Validation gates

## Targeted Kotlin compilation

- Scenario: compile the app after the MainViewModel MVI conversion.
- Invocation: `./gradlew :app:compileDebugKotlin`
- Binary observable: `BUILD SUCCESSFUL in 11s`; `:app:kspDebugKotlin` and `:app:compileDebugKotlin` completed.
- Captured build report: `build/reports/problems/problems-report.html`.

## Full static and package gate

- Scenario: formatting, static analysis, Android lint, and debug APK assembly after the dependency-boundary correction.
- Invocation: `./gradlew ktlintCheck detekt lintDebug assembleDebug`
- Binary observable: `BUILD SUCCESSFUL in 12s` with `1038 actionable tasks`; `:app:assembleDebug UP-TO-DATE` on the final replay.
- Captured artifacts:
  - `app/build/outputs/apk/debug/app-debug.apk` — non-empty (140,543,700 bytes).
  - `app/build/reports/detekt/detekt.txt` — empty, indicating no detekt findings.
  - `app/build/reports/ktlint/ktlintKotlinScriptCheck/ktlintKotlinScriptCheck.txt` — empty, indicating no ktlint findings.
  - `app/build/reports/lint-results-debug.html` and `app/build/reports/lint-results-debug.txt` — generated by `lintDebug`.

`lintDebug` exited successfully. Its report contains only pre-existing repository warnings in the manifest, version catalog, and unused color resources; no changed MainViewModel/MainActivity/build dependency location is named.

## Diff hygiene

- Scenario: whitespace and patch safety.
- Invocation: `git diff --check`
- Binary observable: exit 0 with `DIFF_CHECK_PASS`.
